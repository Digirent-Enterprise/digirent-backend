const CommonValidator = require('./common.validator');
const ObjectValidator = require('./object.validator');


module.exports = {
    //TODO: Migrate validation function to here
    CommonValidator,
    ObjectValidator
}
